#include <stdio.h>

int main() {
    // Write code here
    int count = 0;
    count++;

    int slap = 3;
    slap--;
    
    // Don't change below this line
    printf("count = %d, slap = %d", count, slap);
    return 0;
};