#include <stdio.h>

int main() {
    // Write code here
    int a = 5;
    int b = 3;
    int c = a + b;
    int d = (a > c);
    
    // Don't change below this line
    printf("a = %d, b = %d, c = %d, d = %d", a, b, c, d);
    return 0;
};