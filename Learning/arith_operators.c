#include <stdio.h>

int main() {
    // Write code here
    float a = 5;
    float b = 2;
    float c = a/b;
    
    // Don't change below this line
    printf("a = %2.1f, b = %2.1f, c = %2.1f", a, b, c);
    return 0;
}